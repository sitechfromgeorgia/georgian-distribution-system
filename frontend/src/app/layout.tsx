// Root layout - minimal structure for i18n routing
// The actual layout with fonts, providers, etc. is in [locale]/layout.tsx
export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
